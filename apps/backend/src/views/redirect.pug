extends _layout

block append meta
  meta(http-equiv="refresh" content="3;URL='" + url + "'")

block append styles
  styles .
    main {

    }

block append body
  main
    h4 Redirecting you to #{url}
    a(href=url) Take me there now!